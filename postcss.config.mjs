/** Tailwind v4 is a PostCSS plugin; there is no tailwind.config.js — the theme
 *  is declared in `@theme` inside app/globals.css. */
const config = {
  plugins: {
    '@tailwindcss/postcss': {}
  }
};

export default config;
